package Bridge;

public class Linux implements Platform {
    public void compile(String file) {
        // Linux handling file compilation
        // here an abstrat factory can be used for each file
        return;
    }
}
